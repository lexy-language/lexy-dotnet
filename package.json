{
  "scripts": {
    "test": "dottnet test",
    "build": "dottnet build",
    "update-submodules": "cd src/Lexy.Tests/lexy-language/ && git pull#needs to check why git update --recursive is not working"
  }
}